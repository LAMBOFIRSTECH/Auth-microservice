version: '3.3'
services:
  authentication:
    container_name: DEV-AUTHENTICATION
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    volumes:
      - .:/app
    environment:
      - ENV=development
    
